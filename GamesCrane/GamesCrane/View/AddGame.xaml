<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="GamesCrane.View.AddGame"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:GamesCrane.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:AddGameViewModel/>
    </UserControl.DataContext>

    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid x:Name="GameInput" Width="500" Height="300">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{Binding PathToDisplay}" 
                    HorizontalAlignment="Left" VerticalAlignment="Center" />
            <TextBox Grid.Row="0" Grid.Column="1" Name="GamePath"
                     VerticalAlignment="Center" Text="{Binding NewGame.Path, Mode=TwoWay}"/>
        </Grid>
        <Button Command="{Binding DisplayGameCommand}" Content="Display" />
    </StackPanel>


</UserControl>
